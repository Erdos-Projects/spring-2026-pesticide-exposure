# Core data and notebooks (run from repo root: pip install -r requirements.txt)
pandas>=1.3
numpy>=1.20
requests>=2.25
jupyter>=1.0
notebook>=6.0

# Modeling train_test_split map (choropleth)
plotly>=5.0
scikit-learn>=1.1

# Optional: EDA/build_joint_dataset and joint_eda (CropScape, plotting, etc.)
# Uncomment if you run the full EDA pipeline:
# openpyxl>=3.0
# matplotlib>=3.3
# seaborn>=0.11
# geopandas>=0.10
